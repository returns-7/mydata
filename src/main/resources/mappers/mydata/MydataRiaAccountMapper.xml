<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.mydata.domain.mydata.mapper.MydataRiaAccountMapper">

    <select id="selectByCiHash" parameterType="String" resultType="MydataRiaAccountDTO">
        SELECT mydata_account_id, ci_hash, broker_name, ria_limit, ria_cumulative_sell
        FROM mydata_ria_account
        WHERE ci_hash = #{ciHash}
    </select>

    <insert id="insertAccount" parameterType="MydataRiaAccountDTO">
        INSERT INTO mydata_ria_account (ci_hash, broker_name, ria_limit, ria_cumulative_sell)
        VALUES (#{ciHash}, #{brokerName}, #{riaLimit}, #{riaCumulativeSell})
    </insert>

    <select id="selectByCiHashAndBrokerName" parameterType="MydataRiaAccountDTO" resultType="MydataRiaAccountDTO">
        SELECT mydata_account_id, ci_hash, broker_name, ria_limit, ria_cumulative_sell
        FROM mydata_ria_account
        WHERE ci_hash = #{ciHash} AND broker_name = #{brokerName}
    </select>

    <update id="updateAccount" parameterType="MydataRiaAccountDTO">
        update mydata_ria_account
            set ria_limit = #{riaLimit}
        where mydata_account_id = #{mydataAccountId}
            AND
                ci_hash = #{ciHash}
            AND
                broker_name = #{brokerName}
    </update>

</mapper>
